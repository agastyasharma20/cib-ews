# Versions below are what was actually installed and verified against while
# building each phase (updated as later phases install new packages,
# rather than guessed up front and never re-checked). Packages under
# "PLANNED — not yet installed" have NOT been run against this
# environment yet; their pins are provisional and will be verified (and
# corrected if needed) when that phase is actually built.

# --- Core data stack (verified: Phases 1-7) ---
# pandas was downgraded from 3.0.3 -> 2.3.3 in Phase 7 to satisfy
# lifelines==0.30.3's "pandas<3.0" requirement (flagged as a known future
# conflict back in Phase 3). Before downgrading, every artifact and script
# from Phases 1-6 was re-run under 2.3.3 and produced byte-identical
# results (see git history) — the pipeline does not depend on anything
# pandas 3.0-specific.
pandas==2.3.3
numpy==2.4.6
pyarrow==24.0.0        # parquet I/O for data/raw, data/processed

# --- Modeling (scikit-learn, xgboost, lightgbm verified: Phases 3-5) ---
scikit-learn==1.9.0
joblib==1.5.3          # model (de)serialization to results/models/
xgboost==2.1.3
lightgbm==4.5.0

# --- Explainability — verified: Phase 5 ---
# 0.46.0 (originally pinned) fails to import under numpy==2.4.6
# (deprecated numpy typing API in shap's plotting color-conversion module
# raises TypeError on import). 0.52.0 fixes this.
shap==0.52.0

# --- Survival analysis — verified: Phase 7 ---
lifelines==0.30.3

# --- PLANNED — not yet installed/verified ---
# networkx: Phase 8 (graph features)
networkx==3.4.2
# streamlit / plotly: Phase 9 (dashboard)
streamlit==1.39.0
plotly==5.24.1

# --- Viz (EDA notebooks, static report figures) — verified: Phase 1 ---
matplotlib==3.11.0
seaborn==0.13.2

# --- Notebooks — verified: Phase 1 ---
jupyter==1.1.1
ipykernel==7.1.0
nbformat==5.10.4
nbconvert==7.16.6

# --- Testing — PLANNED, not yet installed/verified ---
pytest==8.3.3

# --- Utilities ---
python-dateutil==2.9.0.post0  # verified (pandas dependency, already in use)
tqdm==4.66.5                   # PLANNED — not yet installed/verified
