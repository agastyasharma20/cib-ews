# Pinned so the whole pipeline (synthetic data -> features -> models ->
# dashboard) reproduces identically on any machine. Versions chosen for
# mutual compatibility on Python 3.12.

# --- Core data stack ---
pandas==2.2.3
numpy==2.1.3
pyarrow==17.0.0        # parquet I/O for data/raw, data/processed

# --- Modeling ---
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0

# --- Survival analysis (time-to-erosion) ---
lifelines==0.29.0

# --- Explainability ---
shap==0.46.0

# --- Graph features (linked-entity / wallet-leakage risk) ---
networkx==3.4.2

# --- Dashboard ---
streamlit==1.39.0
plotly==5.24.1

# --- Viz (EDA notebooks, static report figures) ---
matplotlib==3.9.2
seaborn==0.13.2

# --- Notebooks ---
jupyter==1.1.1
ipykernel==6.29.5

# --- Testing ---
pytest==8.3.3

# --- Utilities ---
python-dateutil==2.9.0.post0
tqdm==4.66.5
